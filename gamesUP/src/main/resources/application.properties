# --- MySQL datasource ---
spring.datasource.url=jdbc:mysql://localhost:3306/gamesup?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
spring.datasource.username=gamesup
spring.datasource.password=gamesup
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- Hibernate / JPA ---
# IMPORTANT : force le dialecte pour contourner l'auto-détection si la DB n'est pas joignable au démarrage
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# En dev pour avancer (quand le schéma sera figé, on passera à validate + Flyway)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# messages d'erreur détaillés en dev
server.error.include-message=always
server.error.include-binding-errors=always

# --- JWT ---
app.jwt.issuer=gamesup
app.jwt.secret=hx6PjN4yF2Q9tA1zUe7rBk5mW8sY3c0vL9qR2nT6pH8dJ1fE4kM7vB2sD5gX9zQ
app.jwt.expiry-minutes=120

python.api.base-url=http://127.0.0.1:8000
python.api.key=dev-reco-key


